import { createPlaceholderRoute } from "@/components/routing";

export default createPlaceholderRoute({
  primaryAction: "Choose command",
  purpose: "Show saved AI commands that produce reviewable app actions.",
  title: "AI Commands",
});
