import { createPlaceholderRoute } from "@/components/routing";

export default createPlaceholderRoute({
  primaryAction: "Review sharing",
  purpose: "Configure explicit AI chat sharing permissions with privacy review.",
  title: "Share chat",
});
