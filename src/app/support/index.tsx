import { createPlaceholderRoute } from "@/components/routing";

export default createPlaceholderRoute({ purpose: "Help and support entry point with privacy-scoped support access.", title: "Support" });
