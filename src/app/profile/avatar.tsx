import { createPlaceholderRoute } from "@/components/routing";

export default createPlaceholderRoute({
  primaryAction: "Edit avatar",
  purpose: "Update profile image or animated avatar with visibility controls.",
  title: "Avatar",
});
