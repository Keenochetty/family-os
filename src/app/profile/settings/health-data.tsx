import { createPlaceholderRoute } from "@/components/routing";

export default createPlaceholderRoute({ purpose: "Review health data categories, records, imports, and metric preferences.", title: "Health data" });
