import { createPlaceholderRoute } from "@/components/routing";

export default createPlaceholderRoute({ primaryAction: "Re-auth required", purpose: "Prepare account deletion with confirmation and backend revocation later.", title: "Delete account" });
