import { createPlaceholderRoute } from "@/components/routing";

export default createPlaceholderRoute({ purpose: "Manage AI data use, private chat defaults, and review-before-save settings.", title: "AI settings" });
