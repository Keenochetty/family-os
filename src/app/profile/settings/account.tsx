import { createPlaceholderRoute } from "@/components/routing";

export default createPlaceholderRoute({ purpose: "Manage account identity after re-auth for sensitive changes.", title: "Account settings" });
