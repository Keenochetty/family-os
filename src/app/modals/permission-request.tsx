import { createPlaceholderRoute } from "@/components/routing";

export default createPlaceholderRoute({ purpose: "Explain permission purpose before requesting access or sharing.", title: "Permission request" });
