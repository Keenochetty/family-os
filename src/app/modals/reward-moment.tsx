import { createPlaceholderRoute } from "@/components/routing";

export default createPlaceholderRoute({
  primaryAction: "Save private",
  purpose: "Moment celebration overlay for safe milestones, avoiding unsafe celebration for medical warnings.",
  securityNote: "TODO: only show celebratory treatment for user-positive events. Keep child photos private and require privacy review before sharing.",
  title: "Reward Moment",
});
